get:
  summary: Health check endpoint
  description: Returns a 200 OK if the server is healthy
  tags:
    - v2
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: string
                example: "ok"
